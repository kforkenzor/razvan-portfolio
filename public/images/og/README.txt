Static Open Graph images (1200x630, < 300 KB) live here.

Nothing uses this folder yet: every route currently falls back to the generated
/api/og/generate route. Drop a home.jpg here and point home.image at it in
src/resources/content.tsx if you want a hand-made preview card instead.

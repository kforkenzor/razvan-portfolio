// content + config, re-exported as one import surface
export { person, social, home, about, projects, presentationSites, work } from "./content";

export { display, routes, baseURL, fonts, style, effects, dataStyle } from "./once-ui.config";
